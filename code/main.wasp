import core.io expose *
import core.types expose *
import core.operators expose *


trait Debug
    pure fun repr() => str
        required


class Jordan is Debug
    name: str

    pure fun repr() => str
        return 'name = {my.name}'


let j = Jordan('jordan')
let g = j.repr()
g.repr()

