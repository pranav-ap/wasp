import my.utils as u

u.print_header('welcome', '*')

class Book
    id: int
    text: str

    fun read() => str
        return my.text


let b = Book(1, 'to be or not to be')

print(b.id + ': ' + b.text)
