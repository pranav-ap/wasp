# Define the string constant.
data $str = { b "hello world", b 0 }

export function w $main() {
@start
    %x =w copy 10      # Initialize
    %x =w add %x, 5    # Reassign - allowed in QBE!
    %x =w mul %x, 2    # x is now 30
    ret %x
}
